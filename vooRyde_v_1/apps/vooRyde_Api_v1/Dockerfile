# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build vooRyde_Api_v1`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t vooRyde_Api_v1`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system vooRyde_Api_v1 && \
          adduser --system -G vooRyde_Api_v1 vooRyde_Api_v1

COPY apps/vooRyde_Api_v1/dist vooRyde_Api_v1/
RUN chown -R vooRyde_Api_v1:vooRyde_Api_v1 .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix vooRyde_Api_v1 --omit=dev -f install

CMD [ "node", "vooRyde_Api_v1" ]
